const stateInit = {
   
};

const reducer = (state = stateInit, action = {}) => {
    switch (action.type) {
      
        default:
            return state;
    }
};

export default reducer;